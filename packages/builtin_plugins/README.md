# mflowy-builtin-plugins

MFlowy 内置能力插件（loaders / cleaners / x_transformer / cross_validation / model / plots / statistic / X_y）——同时是第三方 `mflowy.plugins` 插件包的活参考实现（抄本包 `pyproject.toml` + `hatch_metadata.py`，把 entry points group 改为 `mflowy.plugins` 即可）。
